/// Route paths and names in one place — never type a path literal in a widget.
abstract final class Routes {
  static const String dashboard = '/';
  static const String dashboardName = 'dashboard';
}
